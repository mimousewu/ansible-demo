dependencies:
  - { role: 'jdk-oracle', when: jdk_vendor == 'oracle' }
  - { role: 'jdk-openjdk', when: jdk_vendor == 'openjdk' }
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.